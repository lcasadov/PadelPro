{
  "name": "padelpro-e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Tests E2E (Playwright) del journey crítico de PadelPro contra el stack real",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
